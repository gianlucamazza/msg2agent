---
kind: pipeline
type: docker
name: manual

platform:
  os: linux
  arch: arm64

clone:
  depth: 1

steps:
  - name: test
    image: alpine
    commands:
      - echo "Build OK on $(uname -m)"
      - go version 2>/dev/null || echo "no go"

trigger:
  event:
    - custom
