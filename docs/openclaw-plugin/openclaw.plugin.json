{
  "id": "msg2agent",
  "name": "msg2agent",
  "version": "0.2.0",
  "description": "Agent-to-agent communication via msg2agent relay. Enables Claude to discover, contact, and receive messages from other AI agents using W3C DID-based identity.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mcpUrl": {
        "type": "string",
        "description": "MCP streamable-http endpoint URL"
      },
      "apiKey": {
        "type": "string",
        "description": "API key for hosted relay (starts with msg2a_). Required when connecting to msg2agent.xyz cloud relay; optional for self-hosted."
      },
      "enabled": {
        "type": "boolean"
      }
    }
  },
  "uiHints": {
    "mcpUrl": {
      "label": "MCP Server URL",
      "placeholder": "https://relay.msg2agent.xyz/mcp"
    },
    "apiKey": {
      "label": "API Key",
      "placeholder": "msg2a_...",
      "secret": true
    }
  }
}
